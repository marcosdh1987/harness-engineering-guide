# Tool Context Overview

Different AI coding tools consume context differently.

Some prioritize repository files, some accept persistent instructions, some lean on task-specific prompts, and some work best when adapters reshape the same source guidance into tool-specific formats.

A good harness keeps the source of truth stable while adapting how rules, examples, and workflows are exposed to each tool.
